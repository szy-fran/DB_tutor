documents = [
    "SQL JOIN combines rows from two tables based on a related column.",
    "A primary key uniquely identifies each record in a database table.",
    "Normalization reduces redundancy in relational databases.",
    "A foreign key is a column that references the primary key of another table.",
    "The WHERE clause filters rows based on a condition.",
    "SELECT retrieves data from one or more tables. Use SELECT * to get all columns.",
    "GROUP BY groups rows that share the same value, used with aggregate functions like COUNT or AVG.",
    "A transaction is a sequence of SQL operations treated as a single unit. It either commits or rolls back.",
    "An index speeds up data retrieval but slows down writes.",
    "First Normal Form (1NF) requires that each column contains atomic values and each row is unique.",
    "INNER JOIN returns only rows where the join condition is met in both tables.",
    "LEFT JOIN returns all rows from the left table and matching rows from the right table.",
]
